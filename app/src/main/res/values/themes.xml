<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HomeMusicPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.HomeMusicPlayer" parent="Base.Theme.HomeMusicPlayer">

    </style>


    <!-- Base application theme. -->
    <style name="Theme.SplashScreen" parent="Base.Theme.HomeMusicPlayer">
        ...
        ...</style>

    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0F2027</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.HomeMusicPlayer</item>
    </style>
</resources>